"""Gemini backend application package.

Exposes the top-level sub-modules: main (FastAPI app entry-point),
schemas (Pydantic models), and config (settings).
"""

__all__ = ["main", "schemas", "config"]
